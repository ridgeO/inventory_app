( <%= link_to "Edit Item", edit_location_item_path(@location.id, @item.id) %> | <%= link_to "Delete Item", location_item_path(@location.id, @item.id), method: :delete, data: {confirm: "Are you sure you want to delete #{@item.name}?"} %> )
